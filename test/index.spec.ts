import { } from "../src";
import { story } from "../stories/index.stories";
import chai from "chai/chai.js";
const expect = chai.expect;

describe("Hello world", function() {
  it("should contains only one W", function() {
    // expect(hello_world()["w"]).to.equal(1);
  });
});
